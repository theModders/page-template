{
	"baseurl": {{ site.baseurl | jsonify }},
	"title": {{ site.title | jsonify }},
	"description": {{ site.description | jsonify }},
	"repository_url": {{ site.repository_url | jsonify }},
	"source": {{ site.source | jsonify }},
	"time": {{ site.time | jsonify }},
	"url": {{ site.url | jsonify }}
}